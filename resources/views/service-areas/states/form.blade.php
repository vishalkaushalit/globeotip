<x-app-layout><section class="card"><div class="card-header bg-danger text-white"><h5 class="mb-0">{{ $item->exists ? 'Edit' : 'Add' }} State</h5></div><div class="card-body"><form method="POST" action="{{ $item->exists ? route('states.update',$item) : route('states.store') }}">@csrf @if($item->exists)@method('PUT')@endif<div class="mb-3"><label class="form-label">Name</label><input name="name" class="form-control" value="{{ old('name',$item->name) }}" required></div><div class="mb-3"><label class="form-label">Slug</label><input name="slug" class="form-control" value="{{ old('slug',$item->slug) }}" required></div>@include('service-areas.validation-errors')<button class="btn btn-primary">Save State</button> <a href="{{ route('states.index') }}" class="btn btn-secondary">Cancel</a></form></div></section></x-app-layout>
