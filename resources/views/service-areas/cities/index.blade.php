<x-app-layout><div class="d-flex justify-content-end mb-3"><a href="{{ route('cities.create') }}" class="btn btn-primary">Add City</a></div><section class="card"><div class="card-header bg-danger text-white"><h5 class="mb-0">Cities</h5></div><div class="table-responsive"><table class="table table-bordered align-middle mb-0"><thead><tr><th>State</th><th>Name</th><th>Slug</th><th>Neighbourhoods</th><th>Action</th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->state->name }}</td><td>{{ $item->name }}</td><td>{{ $item->slug }}</td><td>{{ $item->neighbourhoods_count }}</td><td><a href="{{ route('cities.edit',$item) }}" class="btn btn-sm btn-primary">Edit</a> <form class="d-inline" method="POST" action="{{ route('cities.destroy',$item) }}" onsubmit="return confirm('Delete this city and all its neighbourhoods?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@empty<tr><td colspan="5">No cities found.</td></tr>@endforelse</tbody></table></div><div class="m-3">{{ $items->links('pagination::bootstrap-5') }}</div></section></x-app-layout>
