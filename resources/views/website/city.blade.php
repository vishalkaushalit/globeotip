<x-guest-layout>
<section class="page-banner inner-banner"><div class="banner-overlay"></div><div class="container banner-content text-center"><h1>{{ $city->name }}</h1><p>Explore top neighbourhoods in {{ $city->name }}.</p></div></section>
<section class="py-5 bg-white"><div class="container py-md-5"><nav class="mb-5" aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="/">Home</a></li><li class="breadcrumb-item"><a href="{{ route('website.state',['slug'=>$city->state->slug]) }}">{{ $city->state->name }}</a></li><li class="breadcrumb-item active">{{ $city->name }}</li></ol></nav><div class="text-center mb-5"><span class="text-theme-primary fw-bold text-uppercase">Explore Neighbourhoods</span><h2 class="fw-bold display-6">Neighbourhoods in {{ $city->name }}</h2></div><div class="row g-4 mb-5">
@forelse($neighbourhoods as $area)<div class="col-12 col-md-4"><a href="{{ route('website.detail',['slug'=>$area->slug]) }}" class="text-decoration-none"><div class="card h-100 border-0 shadow-sm rounded-4 overflow-hidden feature-hover-card"><img src="{{ $area->imageUrl('main_image', 'https://picsum.photos/seed/'.md5($area->slug.'neighbourhood').'/400/300') }}" alt="{{ $area->name }}" class="card-img-top" style="height:200px;object-fit:cover"><div class="card-body p-4 text-center"><h4 class="fw-bold">{{ $area->name }}</h4><p class="text-theme-muted mb-0">{{ $area->summary ?: 'Discover amazing places in '.$area->name.'.' }}</p></div></div></a></div>@empty<div class="col-12 text-center">No neighbourhoods available.</div>@endforelse
</div>{{ $neighbourhoods->links('pagination::bootstrap-5') }}</div></section>
</x-guest-layout>
